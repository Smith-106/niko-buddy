# 2026-08-20 — T08（control-kernel route() 纯函数 + 720k 单循环 + fast-check 属性面）

> task_id：T08（TASK-P1-03）｜ 阶段：P1 控制面 ｜ 依赖：T07（checkpoint-digest）｜ 验收：A-02.3 / F-10 / C-07

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T08 |
| decision_type | 基线值 / 统计口径 |
| value | **720k 穷举组合数定稿 = 720,000**（14,400 控制维组合 × 50 弧边界用例），与 ainovel-cli `internal/flow/router_exhaustive_test.go` 的 720,000（14,400 基础组合 × 50 boundaryCase）**逐项对照同构**：控制维 10 个（phase 2 × stage 2 × tier 2 × gates 3 × anti_ai_mode 3 × completed 5 × reviewInterval 5 × rewrite 队列 2 × manualReview 2 × foundationMissing 2 = 14,400），弧边界 50 例（1 无边界 + 1 弧中 + 48 弧末 = 2×2×3×4 子维，与 ainovel 同构）。实测单循环耗时 **454ms**（Node 24 / vitest 4.1.10），远低于 ≤5s 硬时间界，**C-07 Rust 移植预案不触发**。 |
| evidence_ref | `QMAI/src/lib/novel/control-kernel.spec.ts`（720k 单循环测试，`npx vitest run control-kernel` 全绿 20/20）；`reference/ainovel-cli/internal/flow/router_exhaustive_test.go` |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T08 |
| decision_type | 裁决人 / 定稿 |
| value | **route() 13 分支优先级链定稿**（自上而下第一个命中）：① halt（终态/无进度）② foundation_fill（规划期缺设定且 tier 已知）③ arbitrate（人工介入，**压过重写队列**——与 ainovel-cli 的 rewrite 绝对优先不同，QMAI 人工介入优先于一切自动派单）④ rewrite（队列头）⑤ arc_transition（弧末五子步：arc_review→arc_summary→volume_summary→expand_arc→new_volume）⑥ global_review（每 reviewInterval 章，lastGlobalReviewChapter 去重）⑦-⑬ 阶段机（context→scene_breakdown→task_brief→draft→review→revision→done；stage=review 时按门控 P0>P1>P2 + anti_ai_mode 三档裁定：consistency fail → revise；anti_ai fail 且 block → revise；quality 永不挡）。 |
| evidence_ref | `QMAI/src/lib/novel/control-kernel.ts`（route() 实现 + 优先级注释）；`QMAI/src/lib/novel/control-sentinels.ts`（ROUTE_ACTIONS 13 分支哨兵） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T08 |
| decision_type | 统计口径 |
| value | **ADR-19 grep 口径注记**：收敛命令 `grep -n 'fetch\|llm\|openai'`（BRE 字面量）对 `shellMode` 标识符（蓝图 §4 强制命名 RouteShellMode，含 "llm" 子串）天然 0 匹配；spec 内 ADR-19 测试改用**词边界** `\b(?:fetch|llm|openai)\b/i` 从严校验（真实模型/IO 调用 token 仍会被拦截），并追加 invoke/node:fs/http/WebSocket 等 token 检查。 |
| evidence_ref | `QMAI/src/lib/novel/control-kernel.spec.ts`（ADR-19 grep 测试）；`docs/niko-buddy-master-blueprint-2026.md` §4（RouteShellMode 类型） |

## 二、验收关联

- A-02.3：720k 单循环全绿 ≤5s（实测 454ms）；route 无 IO/模型调用（grep 0 匹配）；组合数与 ainovel-cli 对照入本文件。
- A-34：本任务（T08）对应 log 条目即本文件。
- C-07：未触发（454ms << 5s 硬时间界）。
