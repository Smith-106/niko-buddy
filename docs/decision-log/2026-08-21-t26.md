# T26 — craft 类型契约落地（canon-craft-fields + beat-model）

> 任务：TASK-P3-26（T26 / F-21+F-22）· 日期：2026-08-21 · 状态：已完成

## 一、decision-log 条目

| 字段 | 值 |
|------|------|
| date | 2026-08-21 |
| task_id | T26（TASK-P3-26） |
| decision_type | 手设参数 |
| value | ① Snyder 15-beat 注册表 15 拍 id/序/幕归属按救猫咪原书节拍表定名（snake_case，即 `edges.beat_label` 合法取值域）；② 名义锚点 nominalPosition 取 110 页页码标记归一 [0,1]（区段型 beat 取区段中点），标注 **PROVISIONAL**；③ 三段式默认比例 25/50/25（区间连续无缝覆盖 [0,1]），标注 **PROVISIONAL**；④ HookType 不定稿为封闭联合——开放注册表（R8/U-05），`string` 承载，挂载点 edges=开端 / episodes=章末（A-23.1/A-23.2）；⑤ ArcStage=U-04 提案 7 值、ConflictCaliber=U-07 提案三值、NarrativeMode=F-26 双值、closure_state=R6 双值 open\|closed，全部以常量注册表+type guard 承载，注释逐处回指 U-xx |
| evidence_ref | `QMAI/src/lib/novel/craft/canon-craft-fields.ts` / `beat-model.ts` / `craft-models.spec.ts`（39 用例全绿）；蓝图 §3 数据面技法列 + §4 接口签名终版 + R6/R8 裁定 |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260821-t26-01 | Snyder 名义锚点与三段式 25/50/25 为 T26 手设 PROVISIONAL 默认值，非 nmem 真源定稿；长篇连载（longform_padding）下节拍分布可能系统性偏离电影页码口径 | T27 爽点量化实测或 T31b L9 回放报告显示锚点系统性偏差 ≥1 个锚点位时复核；调整须经本日志留档并重跑 craft-models/thrill-quantifier 断言 | T31b 报告收口前 |
| DEBT-20260821-t26-02 | `BeatHit` 采用 snake_case wire shape 与蓝图 `{beat_type,...}` 存储列逐字对齐，与域内 camelCase 惯例并存（双命名风格） | 若 T11 Rust DDL 定稿时 serde 改用 camelCase 序列化，则同步重命名并对齐 spec 快照断言 | U-06 canon DDL 定稿前 |

## 三、验证证据

- `npx vitest run craft-models` → 39 passed (39)，1 file passed。
- `npx tsc --build --force` 全仓输出中 craft/ 三文件零错误（150 个既有错误均位于并发任务正在改写的 `src/lib/novel/canon-backfill.ts`（T30b 锚点，读取时内容持续变化）与 `context-engine.ts`（并发修改后复检已消失），非本任务引入）。
- 三文件独立严格模式编译（--strict --noUnusedLocals --noUnusedParameters）：exit 0。
- `npx eslint src/lib/novel/craft/` → exit 0（含 T18 boundaries 门禁）。
