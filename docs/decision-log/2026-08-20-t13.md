# 2026-08-20 — T13 canon_commands.rs（三 IPC 命令 + batch + max_revision + 多项目契约）

> task_id：T13 ｜ 阶段：P1 canon 数据面 ｜ 本文件为 T13 自动判决执行的 decision-log 条目。
> 执行方式：读 TASK-P1-08 + 蓝图 §4 接口终版 + T11/T12 已落地源码，新建 `canon_commands.rs` 并注册 `lib.rs`，直测全绿。

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T13 |
| decision_type | 定稿 / 裁决人 |
| value | **IPC 命令集（蓝图 §4 终版 + T13 增强）**：数据面 5 条 `#[tauri::command]` —— `canon_query`（CanonEdgeFilter 含 known_by?/valid_at_chapter? 过滤）、`canon_ingest_episode`（幂等摄取）、`canon_supersede_edges`（批量封顶+插入）、`canon_query_batch`（多查询单次 invoke）、`canon_facts_known_by`（POV 认知轴便利封装）。核心逻辑与 `#[tauri::command]` 分离（`*_impl` 自由函数），便于 `cargo test` 直测，不依赖 Tauri 运行时 |
| evidence_ref | `QMAI/src-tauri/src/canon_commands.rs`；`QMAI/src-tauri/src/lib.rs`（`mod canon_commands;` + `invoke_handler![..., canon_commands::*]` + `app.manage(CanonCommandState::default())`）；`cargo test canon_commands` 6/6 绿 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T13 |
| decision_type | 手设参数 / 统计口径 |
| value | **响应 `max_revision` 字段**：每条响应（CanonQueryResponse / CanonQueryBatchResponse / CanonWriteResponse / CanonSupersedeResponse）带回当前项目 canon revision。写路径（ingest/supersede）成功后 `bump_revision` 自增；读路径返回 `current_revision`（不 bump）。语义与 T12 `CanonQueryCache` 的 `canon_revision` 键对齐：revision 自增即整缓存失效，供 TS 侧查询缓存失效判定 |
| evidence_ref | `canon_commands.rs`（`CanonCommandState::current_revision` / `bump_revision`）；蓝图 §4 `canon_query_batch+max_revision`；§6 T12 缓存 revision 失效 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T13 |
| decision_type | 裁决人 / 基线值 |
| value | **多项目契约（蓝图 §9 ③ / ADR-16）**：① `project_id` 进每个命令首参签名（JS camelCase = `projectId`）；canon DB 路径 = `{project_id}/.qmai/lancedb`，每项目一库（T11 约定）；② **无跨库 join**：每条命令仅操作单个 `project_id` 库，绝不在一次 invoke 内跨项目读取/合并，多项目聚合由编排层（TS）在 invoke 之外完成；③ **status.json 单实例锁**：以每项目一条 `tokio::sync::Mutex` 写锁（`write_lock`）守写路径串行化（同项目写不并发），读路径不加锁（只读可并发）。revision 计数进程内存、按 project_id 隔离 |
| evidence_ref | `canon_commands.rs`（`CanonCommandState::write_lock` 模块级契约注记）；测试 `multi_project_revision_isolation` 验证 project_id 间 revision 独立 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T13 |
| decision_type | 基线值 |
| value | **依赖边界（QMAI 执行纪律）**：仅包装 T11 `CanonStore`（结构化时态/认知过滤查询 + 幂等摄取 + 批量 supersede）；**未修改 T11/T12 源码**。`canon_query` 走 T11 结构化过滤（§4 契约），T12 `CanonSearch`（FTS+RRF+图遍历）为混合检索增强层，由 T14/T25 投影客户端与 ContextPack 在 packaging 层叠加，不在本 IPC 层混入向量通道。本文件位于 `src-tauri/src/` 顶层（与 `canon_search.rs` 同层） |
| evidence_ref | `canon_commands.rs` 模块级注记；`QMAI/src-tauri/src/commands/canon_store.rs`、`QMAI/src-tauri/src/canon_search.rs`（未改动） |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260820-13 | canon revision 仅为进程内存计数，未持久化到 canon 库；进程重启后 TS 侧缓存需按 revision 0 重新预热 | IPC 重启后首次查询观察到 max_revision 归零、ContextPack 重算 | T25（ContextPack 冻结不变量）前评审是否落盘持久化 revision |
