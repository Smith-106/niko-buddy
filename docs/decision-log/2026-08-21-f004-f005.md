# 2026-08-21 — v2.6 Tier2 Wave-B（F-004 Gate v2 对照文档 + F-005 投影审计 auditTrail）

> task_id：F-004 / F-005 ｜ 阶段：v2.6 Tier2 ｜ wave-b
> 背景：F-004 纯文档对照分析；F-005 在 projection-status.json 内 additive 追加 append-only 审计段

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-21 |
| task_id | F-004 |
| decision_type | 基线值 / 裁决记录 |
| value | docs/gate-v2-benchmark.md 落盘：QMAI Track A/L9 与 StoryForge Gate v2 的粒度/阈值/门控语义对照。核心结论：①两套设计立场不同——QMAI 分层否决制（P0 一票否决）vs StoryForge 单标量加权门（code/rule/model 0.2/0.3/0.5，阈值 0.75，跨级可补偿）；②StoryForge rule 项一半是合同兑现（≈P0 面）一半是追读力（≈P2 面），故 QMAI 只取其 P2 参考分、不引入其门判定入口；③**P2 参考分接入 Track B 可行（MAY），限展示层灰度**（formatP2ReferenceScore 摘要并列展示），禁止参与任何门控判定、禁止与 L9 ≥9.0 换算混用。不改 gate-v2-scoring.ts 现有代码 |
| evidence_ref | docs/gate-v2-benchmark.md（hub 根）；gate-v2-scoring.ts（只读引用）；reference/StoryForge agency/{gate,graders,coordinator}.rs（只读对照）；.workflow/harvest-staging/split-acceptance-tracks.md |

| 字段 | 值 |
|------|----|
| date | 2026-08-21 |
| task_id | F-005 |
| decision_type | 基线值 / 数据契约 |
| value | projection-status.json 内新增 additive `auditTrail?: ProjectionAuditEntry[]` 段（守 ADR-16：非第二份会话状态文件）：entry = { projection, chapter, status: 'committed'\|'rebuild'\|'failed', durationMs, error?, timestamp }。appendProjectionAuditEntry(projectPath, entry) 做读-改-写并 spread 保留全部既有顶层字段（projections/chapters/version 不动）；recordProjectionAudit 纯函数保持内存账本同步，防止循环末尾 saveProjectionStatusLedger 把逐事件落盘条目回滚掉。chapter-ingest 接线两点：runProjection 包装器（committed/failed + durationMs/error 计时）与社区摘要定期重建（成功=rebuild / 失败=failed+error）。审计写失败非致命（logger.warn），不改变 commit-then-project 原子性。滚动窗口策略留 OQ-3（500+ 章无限增长风险已知） |
| evidence_ref | projection-status-ledger.ts + chapter-ingest.ts + projection-status-ledger.spec.ts（19/19 绿，含四分支：单条追加/多条累积/失败含 error/空初始态） |

## 二、验证证据

| 检查 | 结果 |
|------|------|
| `npx vitest run projection-status-ledger` | 1 文件 / 19 测试 passed ✅ |
| `npx vitest run chapter-ingest.behavior` | 77/77 passed ✅（接线未破坏既有行为面） |
| ledger 下游消费方（context-engine/emotional-arcs/resource-ledger/subplot-board/graph-adapter.perf/temporal-memory） | 6 文件 / 268 测试 passed ✅ |
| `npx tsc --build` | **F-005 三文件 0 错误** ✅；仓库整体 exit=2，但残余错误全部位于其他 Wave-B 并发在改文件（chat-panel.spec.tsx ×4 ContextBudget.strategy、context-engine.ts:1199 TS1117、project-store.ts:659 NovelConfig 新字段），与本任务无关且随并发推进漂移（canon-backfill.spec.ts 错误两次运行间自行消失） |

## 三、风险与遗留

- auditTrail 无限增长（~12 事件/章 × N 章）→ OQ-3 滚动窗口策略，v2.6 先建基础。
- rebuildFromCommittedSnapshot 内部暂未按投影粒度发审计事件（恢复路径为整仓重放，无单章语义）；'rebuild' 状态当前由社区摘要定期重建路径产生。
- F-004 结论若升级 reading_power 为 Track B 常驻诊断因子，需独立 decision-log 条目 + 本地对照数据支撑。
