# 103 缺口清单（定稿，证据链闭环）

Date: 2026-09-24. Project: QMAI (niko-buddy v2.11.1 main chain). HEAD 95b40796 clean.
Goal: odyssey/ainovel/maestro 状态机与压缩机制摸底 → 缺口清单 → #104 修复 → #105 全盘 review+测试。

## 取证方法
- 必读：~/.maestro/workflows/run-mode.md(228) + odyssey-review.md(251) + odyssey-base.md(281) 前段。
- 产品代码：control-kernel.ts route()13分支 / control-sentinels.ts / context-compact.ts / context-engine.ts trimContextPack / repair-loop.ts triad / deep-chapter-generation.ts route-runtime / trim-context-plugin.ts / agent/tools/trim-context.ts / pipeline.ts。
- 参考：拆解/ainovel-cli internal/flow/{state,router,advance}.go + stylestat/{stylestat,tracker}.go + agents/ctxpack/{strategy,restore,builder}.go。

## 缺口清单（6 项，均有行级证据）

### (a) 四级压缩/恢复包/熔断器零生产调用
- context-compact.ts: compactContextSections(:282)/buildRestorePack(:222)/createCompactBreaker(:154)/allowHalfOpenProbe(:193)/watchdog(:424) 全部仅经 src/lib/novel/index.ts barrel(:218-227,242) 静态导出。
- 生产消费为零：grep compactContextSections|buildRestorePack|createCompactBreaker|allowHalfOpenProbe 在 src/ 内除 spec/index/context-compact.ts 外零命中。
- context-engine.ts 仅注释引用 CONTEXT_DROP_ORDER 同源顺序，未实际 import/调用。
- 判定：additive 安全容器已落码 + 13 spec 绿，但生产链路未接线。#104 方向：保持 additive 默认等价，不强行切换生产路径；如接线需走三开关 + 项目级隔离。

### (b) route() 仅预览分支 + hardcoded runtime
- route() 13分支互斥纯函数（control-kernel.ts:246），720k 穷举 + 20 spec。
- 生产消费唯一：deep-chapter-generation.ts:63 import + :583 resolveNextStageViaRoute + :513 buildDeepChapterRouteRuntime + :462 DeepChapterRouteRuntime。
- 硬编码（单章视角）：planningTier:""/shellMode:"legacy"/reviewInterval:0/completedChapters:0/pendingRewrites:[]/弧事务全关 → global_review/arc_transition 在单章生成内恒不触发（:510 注释明示）。
- resolveRouteShellMode 缺省 legacy → resolveNextStageViaRoute 返回 null 走既有流水线；route 分支 v8 ignore 预览。
- 判定：内核正确，薄编排 seam 正确，但多章编排（重写队列/周期审阅/弧事务驱动）缺失。#104 方向：补运行时字段透传（NovelConfig 未声明 route_shell_mode/reviewInterval/planningTier，需先加字段），默认保持 legacy 等价。

### (c) AdvanceHold/Steering/Reviewing 无等价物
- grep AdvanceHold|advanceHold|FlowSteering|flowSteering|FlowReviewing 在 src/ 内零命中（除 ainovel 参考）。
- ainovel 语义：FlowReviewing/FlowSteering 返回 nil 让位 LLM/Arbiter；AdvanceHold 一次性暂停三态机（Keep/Consume/ConsumeAndStop）。
- QMAI 现状：manualReviewRequired → arbitrate 分支 + MAX_GATE_RETRY=3 转人工 + emotion 熔断 + continuity critical handoff，覆盖“停”的语义，无“一次性暂停/恢复”语义。
- 判定：缺口成立，但属新机制，需 decision（human judgment）而非 confident edit。#104 方向：记 decision + issue，不强行实现。

### (d) rollup 仅静态，缺增量 Tracker
- mechanical-slop-detector.ts: slopScore(:412)/rollupStyleStats(:847)/bookStyleStatsToText(:906) 仅 barrel 导出；Upsert|styleTracker|incremental style 零命中。
- slopScore 生产消费：candidate-selector.ts + de-ai-batch/scheduler.ts 等（单章检测）；rollup/bookToText 生产消费为零。
- ainovel 语义：Compute 离线全量 + Tracker 按章增量（Upsert/Remove/version/cache）。
- 判定：缺口成立。#104 方向：additive 增量 tracker（纯函数 + 版本号），不碰 slopScore 口径；degraded 初值待真实语料校准（已注释）。

### (e) triad 容器与 deep-chapter 脱节 + 重试预算漂移
- repair-loop.ts: TRIAD_MAX_REWORK=2(:202)/create(:220)/plan/draft/reviewGate(:225/:232/:243)/advance(:257)；生产消费仅 mod.ts:27-31 barrel；deep-chapter-generation.ts 零 triad 引用（grep triad|Triad = 0）。
- index.ts 未导出 triad（仅 mod.ts 导出）；mod.ts 零生产 import（dead barrel，但 spec 引用）。
- 预算漂移：deep-gen MAX_GATE_RETRY=3(:360) vs TRIAD_MAX_REWORK=2；triad 未接线故无 live 冲突，但语义漂移。
- applyContract 4 站点（:1872/:1922/:2242/:2387）均为 fresh stage findings + REPLACE 语义，无累积、无重复（每轮 fresh review 后重算契约）。
- 判定：容器正确 + 14 spec 绿，但编排脱节。#104 方向：保持容器 additive；如协调需统一重试预算（decision：3 vs 2）。

### (f) trim 默认路径 compact 闲置 + dropOrder 双实现
- 生产裁剪路径：context-engine.ts:2824 trimContextPack（本地 fields 表 27 键，与 compact CONTEXT_DROP_ORDER 27 键集合一致、顺序两处 teens 交换）+ contextPackToPrompt；assembleContext 经 deep-chapter-generation.ts:1412 调 contextPackToPrompt(...,32000, excludeOutline:true)。
- trim-context-plugin.ts(96行) 动态 import context-engine，trimContextPackFn/contextPackToPromptFn 注入但 createTrimContextPlugin 零生产实例化；agent/tools/trim-context.ts 虚拟工具走 contextPackToPrompt 直调。
- 缺陷（confident fix 候选）：trimContextPack options `void options` (:2829) → excludeOutline 静默丢弃，大纲恒在 prompt；但 contextPackToPrompt 的 excludeOutline 生效（:2683-2684 + spec:2187）。影响面：仅 trimContextPack 调用方（plugin 注入路径，现零生产实例化）+ prompt 字段；pack 本体裁剪不受影响。
- compactSectionText 导出零消费（grep 仅 context-compact.ts:439 一处）。
- 判定：双实现集合一致但顺序/口径双轨，compact 闲置。#104 方向：修 void options（honor excludeOutline）+ spec 回归；compact 接线保持 decision。

## #104 执行约束
- additive 挂既有锚点；循环导入禁令；机械层零 LLM/零 IO；门控 P0>P1>P2；Draft-first；status.json 唯一真源；finding type 恒 contract；断言不放宽；工作树干净；单写者/commit 纪律。
- 每步 typecheck + 小步 commit；验收命令 ≤5 条 Windows 原生。

## #105 验收
- test:mocks（890文件/13328用例 + graph-view 75/75）+ components（177文件/3055用例）+ build 全绿；acceptance 脚本 R1R2/R3R4/R567 全绿；重复直到绿。
