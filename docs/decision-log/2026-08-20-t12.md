# 2026-08-20 — T12 canon_search.rs（FTS + RRF + 缓存 + petgraph 遍历）

> task_id：T12 ｜ 阶段：P1 canon 数据面 ｜ 本文件为 T12 自动判决执行的 decision-log 条目。
> 执行方式：GLM-5.2-fast stalled 后 flash fallback 自动判决执行（分析既有 WIP 产物 + 迁移 + 升级 + 验证）。

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 基线值 / 统计口径 |
| value | **RRF 公式对照（入 decision-log 强制项）**：TS 侧 `search-adapter.ts:37,424` 多源 wiki 检索 `contribution = weight / (SOURCE_RRF_K=60 + sourceRank + 1)`（K=60 适配多源大池，sourceRank 0-based，`+1` ≡ rank_const=1）；Rust 侧 canon 小池双源（FTS+向量）`score = Σ 1/(rrf_rank_const + rank)`，**rank_const 起点 = 1**（蓝图 §6 T12 / A-06）。同属 RRF 家族，差异在池规模与源数：TS 大 K 平滑多源噪声，canon 紧 rank_const=1 保留头部精度。A-06 劣化兜底亦保留 1 |
| evidence_ref | `QMAI/src-tauri/src/canon_search.rs`（`rrf_contribution` / `rrf_fuse`）；`QMAI/src/lib/novel/search-adapter.ts:37,424`；`cargo test canon_search` 35/35 绿 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 手设参数 |
| value | **窗口衰减形式冻结** `decay(d) = 1/(1+α·d)^β`（蓝图 §9 因子 ⑤），α/β 入 `SearchConfig`：默认 α=0.1、β=1.0（近章高、远章温和衰减的保守起点；T32 在 QMAI 召回池扫 α/β，**形式不换**）。d = \|at_chapter − reference_chapter\|（任一 None → 0 不衰减）；d=0 → 1.0；α=0 → 恒 1.0（关闭衰减）；单调不增（proptest 覆盖） |
| evidence_ref | `canon_search.rs`（`decay` / `chapter_distance` / `window_decay` / `SearchConfig`）；proptest `canon_proptest_decay_monotone` |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 基线值 / 统计口径 |
| value | **查询缓存**：键 = `(filter 规范串, at_chapter, canon_revision)`（`CacheKey`；filter 经 `serde_json` 稳定序列化）；ingest/supersede 触发 revision 自增 → 旧 revision 键自然 miss（整缓存失效语义，无需手动清空）；容量上限 256（`cache_capacity` 入 config），超限按插入序 FIFO 淘汰最早项；命中返回克隆（幂等，proptest 覆盖） |
| evidence_ref | `canon_search.rs`（`CanonQueryCache` / `CacheKey` / `canonical_filter_key`）；proptest `canon_proptest_cache_hit_idempotent` |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 裁决人 / 定稿 |
| value | **petgraph 读侧遍历**：时态/认知过滤后边集物化为内存 `DiGraph<String, ()>`（节点=实体 id，边=source→target），**存储语义零改动**（纯读侧，不落盘）。BFS depth=3（`bfs_depth`，visited 守卫环路安全，输出排序确定性）；连通分量用 union-find 无向投影（组内/组间字典序）；拓扑序用 `petgraph::algo::toposort`，有环返回 `Err(环上节点)`（蓝图未要求强连通分解）。proptest 覆盖 BFS depth 边界（链图恰含深度 ≤ d 节点）与遍历确定性 |
| evidence_ref | `canon_search.rs`（`CanonGraph`）；蓝图 §10.1 petgraph 裁决（L441）；proptest `canon_proptest_bfs_depth_boundary` / `canon_proptest_graph_traversal_deterministic` |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 裁决人 / 定稿 |
| value | **FTS 路径**：真实 LanceDB 0.27 tantivy FTS（T04 spike §5 裁决「FTS 与向量列同表共存可行」）——`ensure_fts_index`（`list_indices` 查重幂等建索引）+ `fts_query`（`Query::full_text_search`，BM25 `_score` 入 raw_score）；建索引失败/表不存在的运行态降级 `fts_query_like`（SQL `data LIKE` 子串召回，与 canon_store「推 down + 精细过滤」一致）。向量召回 `Table::vector_search`（vectorstore.rs 同源 API），raw_score = 1/(1+_distance)。中文分词按 T04 §4 裁决（`tokenizer_verdict` 纯函数落档）：默认内置 tokenizer 对中文退化，T32 调参期按裁决接入 jieba 通道 |
| evidence_ref | `canon_search.rs`（`ensure_fts_index` / `fts_query` / `fts_query_like` / `vector_query` / `tokenizer_verdict`）；集成测试 `canon_search_lancedb_fts_roundtrip` / `canon_search_lancedb_fts_index_idempotent` / `canon_search_lancedb_vector_roundtrip` |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T12 |
| decision_type | 裁决人 / 定稿 |
| value | **文件位置**：`src-tauri/src/canon_search.rs`（与 `types/` 同层，非 IPC 命令模块），`lib.rs` 注册 `mod canon_search;`。既有 WIP 产物在 `commands/canon_search.rs`（已提交 fe6680eb），按任务路径要求 `git mv` 迁移 + `commands/mod.rs` 摘除注册；T13 `canon_commands.rs` 才进 `commands/` 并包装本模块 async 函数 |
| evidence_ref | `git mv src-tauri/src/commands/canon_search.rs src-tauri/src/canon_search.rs`；`lib.rs:1`（`mod canon_search;`）；`commands/mod.rs`（已摘除） |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DE-2026-08-20-07 | T11 canon 三表 DDL 未含 `chunk_text` FTS 索引列 + `embedding` 向量列（T04 spike §6 前瞻要求「三表 DDL 需含」，T11 交付未含）；T12 检索管线（建索引/召回/融合）已就绪并经临时表集成测试验证，但真实 canon 表上 FTS/向量召回需补列或建检索索引表 | T13 canon_commands 接线时补列（additive 迁移链已有）或建检索索引表 | T13 收口前 |
| DE-2026-08-20-08 | FTS 默认 tokenizer（simple）对中文空格分词退化（T04 §4 已知）；`tokenizer_verdict` 裁决规则已落档，jieba 自定义 tokenizer 通道未接入 | T32 调参期按裁决接入 jieba 通道并复测中文召回 | T32 前 |

## 三、验收关联

- `cargo test canon_search` → **35 passed; 0 failed**（RRF 公式/融合/衰减/引擎/缓存命中与失效/FIFO 淘汰/分词器裁决/图遍历 BFS·连通分量·拓扑序 + proptest 融合确定性·源对称·缓存幂等·衰减单调·BFS depth 边界·遍历确定性 + LanceDB 集成 FTS 往返/索引幂等/向量往返）。
- `cargo test` 全量 → **202 passed; 0 failed; 1 ignored**（无回归）。
- `cargo build` → **0 错**（仅剩 dead-code 警告：T13 接入前模块未被消费，与 canon_store 同模式）。
- petgraph 已在 `Cargo.toml`（0.8，T12 注释）+ `Cargo.lock`，无需 `cargo add`。
