# decision-log: LE-2 Phase 2 — CharacterState.isAlive + deathChapter 结构化标记写入端落地

## 任务
TASK-LE-2: LE-1 Phase 2 — 将 `CharacterState.isAlive` + `deathChapter` 从「status 自由文本正则匹配」升级为「数据生产端结构化标记」。

## 决策条目

| 字段 | 值 |
|------|------|
| date | 2026-08-21 |
| task_id | TASK-LE-2 |
| decision_type | U-xx 定稿 |
| value | `applyCharacterStateChangesToStore` 在死亡变更发生时写入 `isAlive=false` + `deathChapter=snapshot.chapterNumber`；引擎 `detectDeadCharacterState` 新增 `isAlive === true` 活态守卫跳过 regex fallback；正则 fallback 保留向后兼容 |
| evidence_ref | `chapter-ingest.ts:1459-1516` / `deterministic-continuity-engine.ts:411-418` / `character-state.ts:19-31` / `deterministic-continuity-engine.spec.ts:478-560` |

## 变更明细

### 1. chapter-ingest.ts — 生产端写入
- 新增 `DEATH_PATTERNS` 常量与 `isDeathStatus()` 辅助函数（与引擎 `deadCharacterPatterns` 同源）
- `applyCharacterStateChangesToStore` 三条路径均写入 `isAlive=false` + `deathChapter`：
  - **parsed 已有角色**：`if (isDeathStatus(changeDesc))` 分支
  - **parsed 新角色**：spread `...(isDead ? { isAlive: false, deathChapter } : {})`
  - **非 colon 回退**：`if (isDeathStatus(change))` 分支
- `status` 自由文本写入保留不变，向后兼容

### 2. deterministic-continuity-engine.ts — 引擎活态守卫
- 新增 `isAliveGuard = c.isAlive === true`，在 `isDeadByText` 前短路 regex fallback
- 消除「死不瞑目」等成语假阳性（AC-002.6）
- `isAlive` undefined 时仍正常回退 status 正则

### 3. character-state.ts — 注释更新
- `isAlive` 与 `deathChapter` 注释从「Phase 2 deferred 升级」更新为「Phase 2 已落地 (LE-2)」
- 标注生产端写入路径与引擎 undefined fallback 契约

### 4. deterministic-continuity-engine.spec.ts — 新增 Phase 2 测试
- `deathChapter` 结构化命中（不走正则 fallback）
- `isAlive=false` 结构化命中
- 旧 store undefined fallback 仍走 status 正则
- 「死不瞑目」成语不再误判 dead（`isAlive=true` 守卫）
- 旧 store 「死不瞑目」仍回退正则（向后兼容）

## 验收证据
- `npx vitest run deterministic-continuity-engine.spec.ts character-state chapter-ingest` → 10 files, 277 tests passed
- 引擎 `isAlive === true` 守卫新增验证通过（AC-002.6）
- `npx tsc --build` 0 新增错误（2 个 pre-existing 未改）