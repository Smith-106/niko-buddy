# 2026-08-20 — T15 canon-dual-write.ts（影子双写 + 持久队列 + T+5 退役）

> task_id：T15 ｜ 阶段：P1 canon 数据面 ｜ 本文件为 T15 自动判决执行的 decision-log 条目。
> 执行方式：读 TASK-P1-10 + 蓝图 §6 T15 + 依赖 T14（canon-graph-client.ts）/ T07（checkpoint-digest.ts）/ T11（canon_store.rs）/ ADR-26（supersession note），新建 `canon-dual-write.ts` + `canon-dual-write.spec.ts` + fixture `canon-pending.jsonl`，vitest 全绿 + tsc --build 0 错。

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T15 |
| decision_type | 裁决人 / 基线值 |
| value | **队列路径裁决（ADR-16）**：持久待写队列运行期路径 = `{projectPath}/.novel/canon-pending.jsonl`（与 `projection-status.json` 同处 `.novel/`）。`src/lib/novel/canon-pending.jsonl` 仅作测试 **fixture**（不承载运行期写入），与运行时路径严格分离，避免把运行态文件误提交进源码锚点目录。JSONL 单行一条 `CanonPendingRecord`，末尾换行，空队列写空串 |
| evidence_ref | `QMAI/src/lib/novel/canon-dual-write.ts`（`canonPendingQueuePath` → `.novel/canon-pending.jsonl`）；`src/lib/novel/canon-pending.jsonl`（fixture 2 条样例：episode + supersede）；`src/lib/novel/projection-status-ledger.ts`（同 `.novel/` 契约参照） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T15 |
| decision_type | 手设参数 / 统计口径 |
| value | **双写设计裁决**：`shadowWriteCanon` 对每个 canon 写操作「旧 snapshot-derived view 适配器（writeLegacy）+ 新 canon_store（writeCanon → T13 IPC `canon_ingest_episode`/`canon_supersede_edges`）」**并行写**（Promise.all），随后 `reconcileCanon`/`reconcileOutcomes` 对账。写失败**不再仅告警**，而是落入持久待写队列。旧 view 侧当前为对账占位 `noopLegacyWriter`（snapshot 事实层已由 ingest 提交点持久化），T16 将替换为真实 snapshot-derived 投影写回；canon 侧为真实 `canonStoreWriter`。两侧 writer 经 `safeWrite` 包裹，抛错统一转 `WriteOutcome`，不中断编排 |
| evidence_ref | `QMAI/src/lib/novel/canon-dual-write.ts`（`attemptDualWrite` 并行双写 + `safeWrite`；`canonStoreWriter` IPC 分发；`noopLegacyWriter` 占位）；ADR-26 §1「互为对账、非平行真相」 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T15 |
| decision_type | 手设参数 / 基线值 |
| value | **队列幂等 + 退避封顶**：每条待写记录以 **SHA-256 digest（T07 `computeCheckpointDigestOf`）为幂等键**去重——同 digest 已入队则保留既有退避调度（attempts/nextRetryAt）仅刷新负载与错误，不重复堆叠。退避 = `BASE=1000ms * FACTOR=2^(attempts-1)`，封顶 `BACKOFF_MAX_MS=300000ms(5min)`（`computeBackoffMs`）。`replayPendingQueue` 重启/下次 ingest 按 JSONL **文件顺序**重放：未到期（nextRetryAt>now）跳过保留，到期重新双写（canon ingest 按 (chapter,digest) 幂等去重），成功出队、失败重新退避保留。此队列是 T18 故障注入矩阵（SIGKILL/部分写/磁盘满/文件锁/OOM/时钟偏移）种子化的前提 |
| evidence_ref | `QMAI/src/lib/novel/canon-dual-write.ts`（`mergePending` digest 去重、`computeBackoffMs` 封顶、`replayPendingQueue` 按序重放）；蓝图 §6 T15「重启/下次 ingest 按序重放」 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T15 |
| decision_type | 裁决人 |
| value | **T+5 退役检查（`retireAfterT5`，ADR-26 §2/§3）**：双写期维持对账态；仅当 `t18PassedAt != null`（T18 垂直切片硬门通过）**且** `currentChapter >= baselineChapter + (tPlusChapters ?? 5)` 时，`canon_store` 取代 snapshot-derived view 成为唯一正典源。T18 未过 → 返回 false、维持双写对账态、P1 不得收口。退役前本模块不触碰旧 view 的「真源」角色 |
| evidence_ref | `QMAI/src/lib/novel/canon-dual-write.ts`（`retireAfterT5`）；`docs/qmai-codex-delivery/adr-26-supersession-note.md`（§2 退役后 canon_store 取代、§3 触发/到期） |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260820-15 | `writeLegacy` 当前为对账占位 `noopLegacyWriter`（恒 ok），未做真实 snapshot-derived 投影写回；双写期旧 view 侧实际未产生独立写入，仅 canon_store 持久化 | T16 钩子接入后观察到双写对账报告旧 view 侧恒一致、且需独立旧 view 写路径时 | T16（chapter-ingest 双写钩子）前替换真实适配器 |
| DEBT-20260820-15b | `reconcileOutcomes` 仅做写层一致性汇总（两侧 ok 与否），未做 T17 的两阶段「按 digest 重放补齐 → 仍不一致才告警」深度对账与差异留痕 | T17 启动、需要重放补齐 + 差异告警时不静默吞差异 | T17 前落地两阶段 reconcile（本模块仅提供写层对账原语） |
