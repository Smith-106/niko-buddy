# T10 decision-log（薄编排化，2026-08-20）

> 关联：T08 control-kernel route()（依赖）+ T09 novel-session-status additive 4 字段（依赖）。
> 完成定义：deep-chapter-golden.spec.ts 零回归；legacy 全等旧行为；精品模式 off 下与 v2.4.8 位级一致。

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T10 |
| decision_type | U-xx 定稿（薄编排化切片裁决） |
| value | deep-chapter-generation.ts 主循环 stage 推进改为『可』读 control-kernel.route() 获取下一步指令→分发；默认 route_shell_mode=legacy（T09 缺省），legacy 分支 resolveNextStageViaRoute 返回 null，既有顺序流水线一字不改运行 → 字节级等价 A-35 不破。stage executor 预留 resolveRoleModel(role, projectConfig) 解析点，默认全角色单模型=现状（T33 注册表落地后仅加注册接入，0 重构）。T09 additive 字段 route_shell_mode / anti_ai_mode 经 resolveRouteShellMode / resolveAntiAiMode 读取（缺省 legacy/off），step_digest 经 computeStepDigest（T07 checkpoint-digest SHA-256 幂等键）计算。route 模式（route_shell_mode="route"）为 T33 注册表落地前预览路径，分支标记 v8 ignore 不计入覆盖率。 |
| evidence_ref | `src/lib/novel/deep-chapter-generation.ts:resolveNextStageViaRoute`；`npx tsc --build` 0 错；`npx vitest run deep-chapter` 204 全绿（含 deep-chapter-golden 1 绿）；`grep -c 'route(' src/lib/novel/deep-chapter-generation.ts = 20 (>=1)` |

## 薄编排化决策点（自动判决说明）

1. **不重构主循环，仅加薄壳 seam**：3785 行 deep-chapter-generation.spec.ts 全量跑主循环，强行把顺序流水线改写成 route() 驱动 loop 风险极高且必然破坏字节级等价。判定采用『additive 薄壳』：默认 legacy 路径与旧代码逐字节一致，route() 仅在显式 route_shell_mode="route" 时介入。
2. **route() 只决策不执行**：严格遵循 control-kernel 硬不变式 ④⑤（不写正文/记忆、不读写状态）。『怎么做』交由 resolveRoleModel 预留解析点（默认单模型），与蓝图 §4 接口终版一致。
3. **T09 字段接入方式**：route_shell_mode / anti_ai_mode 走 reader 解析（input 优先于 novelConfig，项目级隔离），缺省 legacy/off = 现状；step_digest 经 T07 幂等键计算，落 status.json 真源的写动作仍在 novel-session-status 生命周期层（HARD-1 守恒，本文件不越界写状态）。
4. **覆盖率处理**：route 模式分支 v8 ignore；新增纯函数（resolveRoleModel / resolveRouteShellMode / resolveAntiAiMode / buildDeepChapterRouteRuntime / computeStepDigest / resolveNextStageViaRoute legacy 路径）由新建 `deep-chapter-generation-route-shell.spec.ts` 单测覆盖。
5. **Draft-first 守恒**：未新增任何 AI 输出路径，既有 pending/ready 草稿边界（novel-session-status 生命周期）一字未改。

## 债条目（如适用）

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260820-01 | route 模式（route_shell_mode="route"）当前为预览路径，未接 T33 角色注册表，step_digest 未回流 status.json（仅计算未落盘）。 | T33 注册表落地后，将 resolveRoleModel 接入注册表 + 将 computeStepDigest 结果经 onCheckpoint/生命周期层写 status.json.step_digest。 | P1 收口 / T33 前 |
