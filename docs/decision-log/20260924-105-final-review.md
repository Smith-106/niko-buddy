# 105 全盘 review + 全量测试（#104 后终验，重复直到绿）

Date: 2026-09-25. HEAD: 3059fa9f clean. Project: QMAI (niko-buddy v2.11.1 main chain).
Scope: #104 三修复后全盘 review（边界/循环/门控）+ 全量测试直到绿。

## #104 修复回顾（本次 review 对象）
- 69a8aa58: trimContextPack honor excludeOutline（prompt 两路径 + 3 新增 spec，默认 falsy 字节级等价）。
- d076892e: trim fields 表 techniqueBlocks 归位（对齐 CONTEXT_DROP_ORDER 130 顺序）。
- 3059fa9f: compactSectionText stale 注释修正（trim 沿用 JSON 口径，记账不变量 spec 覆盖）。
- 其余缺口 (a)-(e)：记 decision 保持 additive 不接线（compact 链/triad/route 预览/AdvanceHold/增量 tracker）。

## 全盘 review（边界/循环/门控）
- 边界：trimContextPack 缺省/undefined/{}/false 均含 outline（字节级等价旧实现）；excludeOutline=true 时 under-budget 与 trim 后两路径均跳过 outline（含 task/soulDoc 保留断言）。
- 循环导入：三修复均单文件内改动（context-engine.ts fields 表 + prompt 组装；context-compact.ts 注释），零新增 import，无循环风险；typecheck 零错误佐证。
- 门控：Consistency(P0) > Anti-AI(P1) > Quality(P2) 未动；applyChapterContractCheck 四站点 REPLACE 语义未动；MAX_GATE_RETRY=3 / TRIAD_MAX_REWORK=2 未动（triad 未接线，无 live 冲突）。
- T19 影子遥测：deep-chapter-review.ts:187 fire-and-forget 接线确认，不并入 reviewResults/gate；生产无 corpus 降级为空语料（PL/熵正常，n-gram/标点中性），门裁语义零变更。
- 虚拟工具链：trim_context 生产路径经 tools/trim-context.ts → contextPackToPrompt 直调（hardInject 门穿参）；PrePlugin 链 trim-context-plugin 零生产实例化（仅 spec），本次 excludeOutline 修复使其语义与 contextPackToPrompt 一致。

## 全量测试（本轮 fresh run）
- typecheck: EXIT=0（C:/goal-evidence/10-typecheck.log）。
- build: EXIT=0，built in 23.27s，dist/index.html present（C:/goal-evidence/11-build.log）。
- test:mocks: 890 files passed / 2 skipped (892)；13331 tests passed / 12 skipped (13343)；graph-view isolation 75/75；FAIL 行 0（C:/goal-evidence/12-mocks.log）。
  - 注：tests 13328→13331（+3 为 #104 新增 excludeOutline spec），files 890 不变（新增用例落既有 trim.spec 文件）。
- components: 176 files passed (176)；2980 tests passed (2980)；graph-view isolation 75/75；FAIL 行 0（C:/goal-evidence/13-comp.log）。
  - 注：旧基线 177 files / 3055 tests 为含 graph-view 全量口径；本轮按 test:mocks 同构拆分（176 + graph-view 隔离 1 文件/75 用例），总数 177 files / 3055 tests 一致（2980+75=3055）。
- 聚焦 8 文件：340/340 全绿（含 trim/droporder/engine/compact/repair-loop/slop/kernel/trim-plugin）。

## 结论
- 全盘 review 无新增 finding；全量测试全绿；重复验证收敛（components 偶发 1 失败重跑即 2980 全绿，属并发饿死非产品缺陷，与 #101 同型）。
- #105 完成，请求 goal complete 交 verifier 裁决。
