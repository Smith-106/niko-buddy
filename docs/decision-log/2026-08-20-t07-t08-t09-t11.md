# 2026-08-20 — T07/T08/T09/T11（wave-1 批次 A+B 落地决策与债条目）

> task_id：T07/T08/T09/T11 ｜ 阶段：P1 控制面 ｜ 本文件为 wave-1 共识式协作首波（批次 A+B）的 decision-log 条目。
> 共识来源：`flash-w1.md` + `hy3-v1.md`（GLM-5.2-fast 端点三次故障，2/3 共识）；共识裁决 `consensus-verdict.md`。

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T07 |
| decision_type | 基线值 / 统计口径 |
| value | checkpoint-digest.ts 用 `crypto.subtle.digest("SHA-256", ...)` 异步 API；输入规范化=键序稳定 JSON 序列化（`Object.keys().sort()` 后 `JSON.stringify`），相同语义对象产相同 digest；16/16 测试绿 + tsc 0 错 |
| evidence_ref | `QMAI/src/lib/novel/checkpoint-digest.ts`；`checkpoint-digest.spec.ts`（16 tests passed） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T08 |
| decision_type | 基线值 / 手设参数 |
| value | control-kernel.ts `route(state)` 纯函数 13 分支互斥优先级链（halt→foundation_fill→arbitrate→rewrite→arc_transition→global_review→7 阶段机）；720k 穷举 ≤5s 用**批收集失败+末尾单次断言**规避 vitest 默认 5000ms 同界超时；实测 885ms 全绿；`firstFailedGate` 辅助 P0>P1>P2；fast-check 属性面（守恒/纯函数 DeepEqual/确定性）三属性 |
| evidence_ref | `QMAI/src/lib/novel/control-kernel.ts`；`control-sentinels.ts`（`EXHAUSTIVE_COMBINATION_COUNT=720_000` / `EXHAUSTIVE_TIME_BUDGET_MS=5_000`）；`control-kernel.spec.ts`（88 tests passed，含 720k 循环计时） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T08 |
| decision_type | 裁决人 / 定稿 |
| value | C-07 Rust 移植预案**暂不触发**：720k 实测 885ms（预算 5000ms，余量 82%）；若后续 T10 deep-chapter 接入后 route 调用频率致总循环上升再复测，超预算则前置 C-07 |
| evidence_ref | `control-kernel.spec.ts:606-609`（`expect(total).toBe(720_000)` + `expect(elapsedMs).toBeLessThanOrEqual(5_000)`） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T09 |
| decision_type | 基线值 / 统计口径 |
| value | novel-session-status.ts additive 4 字段：`step_digest?` / `route_shell_mode?` / `canon_migration?` / `anti_ai_mode?`（可选，默认 undefined）；zod passthrough schema 作加载护栏（校验+默认回填+保留未知键，TS 类型唯一源）；写路径原子替换不动；三开关项目级隔离按 projectId 注记；status.json 禁走 tauri-plugin-store |
| evidence_ref | `QMAI/src/lib/novel/novel-session-status.ts`；`novel-session-status.t09.spec.ts`（10 tests passed）；`package.json` zod ^3.25.76 |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T11 |
| decision_type | 基线值 / 统计口径 |
| value | canon 三表：`canon_entities` / `canon_edges` / `canon_episodes`；episodes 表兼任 ingest_log（`(chapter_number, digest)` 去重幂等键，不另建独立 ingest_log 表，避免第二份会话/摄取状态文件——ADR-16）；ingest_digest 去重 = 写前检查 `(chapter, digest)` 集合命中跳过；schema_version 迁移链 `migrate_up`（`Table::add_columns` additive）+ `migrate_rollback`（`Table::version/checkout/restore` 物理回退）+ dry-run；proptest 覆盖时态不变量/迁移幂等/去重 |
| evidence_ref | `QMAI/src-tauri/src/types/canon_types.rs`；`src/commands/canon_store.rs`（`migrate_up` L775 / `migrate_rollback` L844 / `IngestKey` L892）；38 tests passed |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T11 |
| decision_type | 外部佐证引用 |
| value | LanceDB 0.27 `Table::add_columns(NewColumnTransform::SqlExpressions, None)` 支持列追加（spike T04 未验证列演化，T11 实测通过：`canon_lancedb_migrate_up_adds_columns_then_rollback` 38/38 绿）；rollback 用 LanceDB 版本化 `checkout/restore`（物理回退 schema） |
| evidence_ref | `canon_store.rs:830`（add_columns）；测试 `canon_lancedb_migrate_up_adds_columns_then_rollback`（v1(7)→10 列→rollback 7 列） |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DE-2026-08-20-03 | T08 批断言设计债：720k 循环改批收集失败+末尾单次断言规避超时，偏离「逐组合 4 断言」字面（语义保留：4 条件判真仍全检），未跑 long-run 10× 复测稳态 | T10 接入后 route 调用频率上升时复测稳态 + 10× 稳态方差 | T18 硬门前 |
| DE-2026-08-20-04 | T11 LanceDB schema 演化仅验证 additive ADD COLUMN；复杂迁移（重命名/类型变更/列删除）未覆盖，rollback 仅靠版本化 checkout（无 down-script） | T12+ 遇非 additive 迁移需求时补充 down-script 设计 | P2 前（T13 IPC 收口前） |
| DE-2026-08-20-05 | T11 `ingest_digest` 去重为写前内存集合检查（单进程串行缓解竞态），无 DB 唯一索引；多进程并发写入未覆盖 | 多实例并发场景出现 或 LanceDB 支持唯一约束时补 | P3 写作域多并发前 |
| DE-2026-08-20-06 | GLM-5.2-fast 端点三次故障（Provider finish_reason: error），wave-1 共识式协作实际为 2/3 模型（flash + hy3）共识；GLM 观点缺失 | GLM 端点恢复后补一轮 GLM 独立分析交叉验证 | 下次共识式协作前 |

## 三、验收关联

- 全量回归：`npx vitest run` → 499 文件 / 8933 测试全绿（exit 0，基线 8873 → +60 新增）；`npx tsc --build` 0 错；`cargo test canon_` 38/38 绿。
- 720k 性能：实测 885ms ≤ 5000ms（预算余量 82%），C-07 Rust 移植预案暂不触发。
- 共识裁决文件：`.workflow/sessions/unfinished-tasks-execute-w1-20260820/runs/20260820-001-execute-w1/outputs/consensus/consensus-verdict.md`
