# #93 全量 flaky 根因确立 + story-compass-io 测试缝修复（稳定性专项）

| 字段 | 值 |
|------|-----|
| date | 2026-09-24 |
| task_id | #93 |
| decision_type | U-93 定稿 |
| value | (1) 根因（#92，静态证据链闭环，免探针）：vite.config.ts T18 注释实证 chapter-ingest/reset-project-state 冷加载 ~7.2s，testTimeout 15s ≈ 2× 实测量，全量 4-worker 并发 CPU 争抢下可击穿；chapter-ingest.ts 静态 import 拖入 llm-client/wiki-store/graph-adapter/projection-status-ledger 等巨型图，`collectCompletionChecklistInput` 首行即 `await import("./chapter-ingest")`，spec 的 fs mock 对模块加载 stall 零覆盖 → 纯超时（断言从未执行）；三轮失败集互不相同且递减，kb-routing-view.spec 自带 R7 注释承认 doMock+resetModules 全量并发偶发击穿 → 负载抖动类 flaky，非产品缺陷。(2) 修复：`collectCompletionChecklistInput` 新增第 4 可选参 `deps: CompletionInputCollectDeps = {}`（`listSnapshots` 注入缝，默认动态 import 路径，生产零行为变化；`CompletionInputCollectDeps` 已进 index.ts 类型导出）；`story-compass-io.spec.ts` 四调用点全部注入微型 lister（与 listSnapshots 同解析口径 outline-NNN→负号，输入仍由 fsMocks.listDirectory 驱动，降级路径照测）。(3) 验证：单文件 5/5 通过总时长 1.40s（修复前单项即 15s 超时）；聚焦 13 文件 374/374；`npm run typecheck` 0 错误（TYPECHECK_EXIT=0）；全量 `npm run test:mocks` 888 passed/4 failed，其中 story-compass-io 本轮 0 失败（修复实证）；剩余 reset-project-state×2/kb-routing-view×1/graph-view×1 均为 15–86s 级超时，隔离重跑 kb/graph 全绿，reset 单文件 stash 对照（stash 掉本轮全部改动仍同项 30s 超时）证明为既有环境 flaky、与本轮改动零因果。门控优先级与 Draft-first 不变；四维度评分无变化（零产品行为变化，无需重评）。 |
| evidence_ref | `story-compass.ts` CompletionInputCollectDeps + listSnapshotsFn；`story-compass-io.spec.ts` DEPS()；vite.config.ts T18/testTimeout 注释；全量日志 /tmp/test-mocks-20260924.log（888 passed/4 failed，FAIL 行原文）；隔离日志 /tmp/iso3-2.log；stash 对照日志 /tmp/reset-stash.log |

## 债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260924-89b | `applyChapterContractCheck` transitional 恒 false（过渡章判定 deferred，结转） | 滚动规划消费 compass 时 | 后续 Round |
| DEBT-20260924-93a | reset-project-state.spec 首项 30s 超时既有 flaky（stash 对照证与产品改动无关；T18 冷加载 ~7.2s 背景） | 改动 reset 模块图或加 workers 时复查 | 后续 Round |
