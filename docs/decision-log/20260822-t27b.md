# T27b — technique-compiler.ts（nmem space → 规则包，版本化 + 离线降级）

> 任务：TASK-P3-27b（T27b / F-20 / A-04.6）· 日期：2026-08-22 · 状态：已完成

## 一、decision-log 条目

| 字段 | 值 |
|------|------|
| date | 2026-08-22 |
| task_id | T27b（TASK-P3-27b） |
| decision_type | 统计口径 |
| value | ① **版本化口径**：nmem memories 不暴露可变版本计数器 → 记忆级版本 = 快照版本号 `NMEM_SNAPSHOT_VERSION`（schema/语料变更触发全量重编译时整体 +1，蓝图 §8 P3）；skill 级版本用 nmem 原生 `verification.version`(=1) + `content_hash`(sha256) 承载；追溯键 = `(snapshotVersion, memoryId)`，每包元数据带 `sourceSnapshotVersion + sourceMemoryIds`。② **hook 开放注册表首批取值（R8/U-05 未定稿，string 承载）**：章末 11 型挂 episodes（维兰德 memory 28dc7918：预示冲突/秘密/重要决定或誓言/宣布震惊事件/激烈情绪/颠覆性突转/新想法/未回答的问题/神秘对白/预言/转折点）+ 开端 10 方案挂 edges（莫雷尔 memory 786b0422：对话/逸事/疑问/悬念/主题/设定/有力首句/人物描写/转机/伏笔），共 21 条，同名型跨挂载点隔离。③ **离线降级语义**：探活（GET /health，status==ok）失败或 live 抓取/编译抛错 → 优雅中止回落入仓快照 `NMEM_SNAPSHOT`，快照路产物与离线编译 DeepEqual（功能不退化）；网络函数一律显式注入（`NmemFetchDeps.fetchJson`），模块零隐式网络调用，runtime 永不直连。④ **主角 canon 技法字段写入口径**：零 IO 模块产出 `CanonCraftFieldWrite` 载荷（fields+字段级 provenance），由 F-20 下游摄取管线经既有双写通道落库；wish/motive/mckee_ghost/arc_stage 四硬门缺件即 TypeError fail-fast（填充率 100% 为硬门非事后统计）。⑤ arc_stage 字段目标归属 craft.mckee-eight-fundamentals 包（弧光阶段推进语义）。 |
| evidence_ref | `QMAI/src/lib/novel/craft/technique-compiler.ts` / `nmem-snapshot.ts` / `technique-compiler.spec.ts`（39 用例全绿）；快照实测采集自 nmem space=space（server v0.10.67 探活通过，2026-08-21T15:30:23Z），9 源 = 8 memory（20de3c24/04644331/84c7f90a/akers-ghost…/28dc7918/786b0422/edgerton-…/94a6af29）+ 1 skill（skill_f8e81e050000 v1） |

## 二、与蓝图的偏差注记（须复核项）

1. **「蔡骏五法开端」未入注册表**：蓝图 U-05 提及「蔡骏五法/11 型章末钩子枚举（F-20 摄取编译）」，本轮 nmem space 实测检索（"蔡骏 开端 悬念"等查询）仅命中蔡骏联想训练法/故事架构类记忆，未命中开篇五法专条。按 R8 开放注册表语义只注册 nmem 实际存在语料（莫雷尔十方案承接开端钩子位），U-05 定稿时增补即可，无需改结构。
2. **验证命令中的 `--offline` 后缀**：任务行写 `npx vitest run technique-compiler --offline`，但 vitest 无该 CLI 旗标；离线能力的实质保障是——测试为纯函数契约测试（网络函数注入 fake，永不触网），收敛命令 `npx vitest run technique-compiler` 本身即全程离线可跑。
3. **contentExcerpt 非全文搬运**：快照只收录编译所需最小操作口径面（控制体积与版权面），全文真源仍在 nmem server；重编译脚本（后续 T28/T34b 工具链任务）负责刷新。

## 三、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260822-t27b-01 | `fetchLiveSnapshot` 的 REST 抓取形态（/api/memories/search 查询串与返回 shape）按当前 server v0.10.67 实测假设编写，nmem 升级可能漂移；live 路仅编译期工具链使用，runtime 不受影响 | 重编译脚本首次实跑 live 路失败且 fallbackReason 显示 shape 断言错误时，按当版 server 契约修正并对齐 spec fake | T28 literary-craft-pack 编译接线前 |
| DEBT-20260822-t27b-02 | 八素质 slot_names 英文键（willpower/versatility/underdog_position/empathy_core/duplicity/foreground_depth/change_capacity/epiphany_insight）为本轮从 memory 84c7f90a 摘译的 **U-04 提案回填**，非定稿命名 | U-04 定稿（nmem 麦基书摘记忆回填命名）后同步改名并升快照版本重编译 | U-04 定稿前 |
| DEBT-20260822-t27b-03 | 快照 capturedAt/serverVersion 为手工固化值（2026-08-21T15:30:23Z / 0.10.67），无自动刷新机制 | T34b 工程化清单落地重编译脚本（探活→抓取→validate→diff→升版提交）后消除手工步 | P3 收口前 |

## 四、验证证据

- `npx vitest run technique-compiler` → **39 passed (39)**，1 file passed（含确定性 DeepEqual、溯源守卫全违规路径、21 条 hook 注册表、四硬门填充率 100%、降级路径 5 例全 fake 网络）。
- `npx vitest run craft-models technique-compiler` → 78 passed (78)，共享目录无回归。
- 三文件独立严格模式编译（`tsc --noEmit --strict --noUnusedLocals --noUnusedParameters …`）：exit 0。
- 全仓 `npm run typecheck` 当前报 9 错，**均位于并发任务正在改写的文件**（anti-ai-candidate-pool.* 7 处 = P2-19 语料轨；craft/arc-tracker.spec.ts、craft/thrill-quantifier.spec.ts 各 1 处 = w2-t27 并行任务 WIP），本任务三文件零错误。
- `npx eslint`（三文件，含 T18 boundaries 门禁）→ exit 0。
