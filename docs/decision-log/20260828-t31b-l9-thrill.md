# A-34 决策日志 — T31b L9 回放执行 + 爽点量化有效性检验

```yaml
date: 2026-08-28
task_id: TASK-P4-31b
decision_type: 基线值
wave: Wave-6 (P4)
model: ox-alpha
verified: node scripts/thrill-retention-correlate.js EXIT=0 三项检验全 PASS + npx tsc --build 0 错
```

## 决策

1. **L9 回放协议 baseline（六维 overall 中位 ≥9.0）**：10 章 route() 决策状态序列 → 六维投影评分，等权中位 = 9.25 ≥ 达标线 9.0。六维全部 ≥9.0，最低 thrill/pacing/character/pull = 9.0。融合权重参数化双模式已实现（等权=门控真源；加权仅 diagnostic 不改门控）。

2. **判官池局限（PENDING 标记）**：当前环境仅单模型可达，判官池 ≥2 异模型来源标记为 **PENDING**。离线回放 fixture（T31 offline-replay.js 模式）补位，六维评分由纯函数从 route() 决策状态序列投影得出，而非真实 LLM 判官逐维打分。报告中明确标注局限，不粉饰。

3. **爽点量化有效性检验 baseline（fixture）**：
   - 检验 A（Spearman）：ρ = 0.934 ≥ 0.6，N=40 ≥ 40，2 本书 ≥ 2，Bootstrap 95%CI [0.840, 0.969] → **PASS**
   - 检验 B（弧闭环率配对）：前 20 章 0.329 → 后 20 章 0.646，p = 0.0001 < 0.05 → **PASS**
   - 检验 C（Accept vs Reject MWU）：Accept 0.786 vs Reject 0.436，p ≈ 0.0000 < 0.05 → **PASS**
   - 全部数据为 fixture（确定性种子 20260820/20260821/20260822），真实语料待补充

4. **运营期复验口径（A-13 分期）**：`scripts/thrill-retention-correlate.js` 已预留 N≥200 口径，P6 后执行 `node scripts/thrill-retention-correlate.js` 即可复验，不阻塞本任务。

5. **检验脚本纪律**：可重跑（确定性 PRNG），JSON 输出 CI 消费，EXIT=0（PASS）/ EXIT=1（FAIL）/ PENDING 不阻塞。

## 定稿参数

| 参数 | 值 | 类型 |
|------|-----|------|
| 六维等权中位达标线 | 9.0 | 门控真源 |
| 六维加权模式 | 仅 diagnostic 不改门控 | 权重参数化 |
| 判官池来源 | 1（单模型 + 离线 fixture）+ PENDING 标记 | 局限 |
| Spearman 达标线 | ≥0.6 | 门控 |
| 弧闭环率配对 p | <0.05 | 门控 |
| Mann-Whitney U α | 0.05 | 门控 |
| 运营期 N 目标 | ≥200 | P6 后 |
| 运营期跨书目标 | ≥5 | P6 后 |

## 边界

1. 全部检验数据为 fixture（确定性生成），统计显著性不代表真实语料。
2. 判官池仅 1 模型来源，未实现真多模型交叉验证。
3. 六维评分为投影映射（非真实 LLM 判官逐维打分），投影假设未经验证。
4. 留存率与 Accept/Reject 为模拟值，无真实埋点数据。

## 债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260828-t31b-01 | 判官池不足 2 异模型来源：当前仅单模型+离线 fixture 补位，未实现真多模型交叉验证 | 多模型环境可用（DeepSeek + Claude 或 GPT + 本地模型） | P6 前 |
| DEBT-20260828-t31b-02 | 爽点量化有效性全 fixture 数据：真实语料/留存率/埋点数据未接入，统计显著性不代表真实场景 | 运营期 N≥200 真实语料积累 | P6 运营期 |
| DEBT-20260828-t31b-03 | 六维评分投影映射未经验证：route() 决策质量与六维评分正相关的假设无实证 | 真实 LLM 判官逐维评分 + 人工校准 | P6 运营期 |
| DEBT-20260828-t31b-04 | 运营期复验未执行：N≥200、≥5 本书、跨书 CV<0.5 等目标待 P6 后验证 | 运营期数据达 N≥200 | P6 后 |