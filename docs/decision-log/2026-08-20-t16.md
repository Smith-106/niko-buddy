# 2026-08-20 — T16 chapter-ingest 双写钩子（接入 T15 canon-dual-write）

> task_id：T16 ｜ 阶段：P1 canon 数据面 ｜ 本文件为 T16 自动判决执行的 decision-log 条目。
> 执行方式：读 TASK-P1-11 + 依赖 T15（`canon-dual-write.ts`）+ T07（`checkpoint-digest.ts`）+ T11（`canon_store.rs`，`canon_ingest_episode` IPC），在 `chapter-ingest.ts` 的 `validateCanonConflicts` 调用点后**单点**接入影子双写钩子；vitest `chapter-ingest` 全绿（含新增 17 例）+ `tsc --build` 0 错 + `chapter-ingest.ts` 覆盖率 100%（stmt/branch/func/line）。

## 一、决策条目

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T16 |
| decision_type | 裁决人 / 基线值 |
| value | **单点接入 + 双保险守卫**：双写仅在 `ingestChapter` 的 `if (snapshot)` 块内、`validateCanonConflicts` 调用点之后、memory-op 演练之后**单点**调用 `runCanonDualWriteHook(options.canonDualWriteDeps, pp, fm, snapshot)`。`ingestChapter` 已有的 `isFinalChapter` early-return（`not_final`）先拦截 pending/draft/revised/outline/archived；钩子内再以 `isCanonDualWriteEligible(fm)`（仅 `final`/`accepted` 放行）二次守卫。两层守卫保证「**reject 先于双写**」：任何未通过 accept 的章（pending/reject/draft）都**绝不触达 canon 双写**。钩子失败**非致命**（catch → `logger.warn`），不阻断 ingest 主流程 |
| evidence_ref | `QMAI/src/lib/novel/chapter-ingest.ts`（`runCanonDualWriteHook` 调用点，`if (snapshot)` 内 memory-op 之后；`isCanonDualWriteEligible` 守卫）；`QMAI/src/lib/novel/chapter-ingest.t16.spec.ts`（reject 先于双写断言：draft 章 early-return 不触达双写、deps 未注入/非 eligible/无新事实均不写 canon） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T16 |
| decision_type | 手设参数 / 统计口径 |
| value | **依赖注入（DI）向后兼容**：通过 `IngestChapterOptions.canonDualWriteDeps?: CanonDualWriteDeps` 注入。运行期由调用方注入 `defaultCanonDualWriteDeps()`；**缺省 undefined → 钩子空操作**（旧调用方/现有 spec 不触发双写，零回归）。双写编排本身复用 T15 `shadowWriteCanon`（旧 view 并行写 + 新 canon_store 写 + reconcile + 失败落 `.novel/canon-pending.jsonl` 持久队列），本任务不重造编排 |
| evidence_ref | `QMAI/src/lib/novel/chapter-ingest.ts`（`IngestChapterOptions.canonDualWriteDeps`；`if (!deps) return` 早退）；`QMAI/src/lib/novel/canon-dual-write.ts`（`shadowWriteCanon` 编排入口） |

| 字段 | 值 |
|------|----|
| date | 2026-08-20 |
| task_id | T16 |
| decision_type | 手设参数 / 基线值 |
| value | **操作派生（snapshot → canon episode）**：`buildCanonDualWriteOps(snapshot)` 逐条取 `snapshot.newCanonFacts`，每条转换为一个 `CanonDualWriteOp`：`canonPayload = { kind: "episode", episode: { id: "ch{ch}-fact{i}", chapter_number, entity_id: snapshot.chapterId, summary: fact, digest } }`，`legacyPayload = { kind: "snapshot_fact", chapterNumber, fact }` 占位（T15 `noopLegacyWriter` 对账），`digest` 由 T07 `computeCheckpointDigestOf({ chapter, fact })` 派生（SHA-256 幂等键，与 T11 `ingest_digest` 去重契约一致）。`newCanonFacts` 为空 → 操作集空 → 钩子直接返回（无 IO）。`now` 默认 `Date.now()`，可注入以确定性测试 |
| evidence_ref | `QMAI/src/lib/novel/chapter-ingest.ts`（`buildCanonDualWriteOps`）；`QMAI/src/lib/novel/checkpoint-digest.ts`（`computeCheckpointDigestOf`）；`QMAI/src/lib/novel/chapter-ingest.t16.spec.ts`（`buildCanonDualWriteOps` 派生断言：digest 命中 SHA-256、episode 形状） |

## 二、债条目

| 债 ID | 描述 | 偿还触发 | 到期阶段 |
|-------|------|----------|----------|
| DEBT-20260820-16 | `buildCanonDualWriteOps` 的 episode 映射为 T16 阶段最小实现：`entity_id` 暂用 `snapshot.chapterId`（章级 POV 占位），未使用 T15 `supersede` 负载路径；真实 canon 事实 schema（`entity_id` 取具体实体、是否拆分为 supersede 边）待 canon 事实模型定稿后校准 | canon 事实模型定稿、需要实体级 POV / supersede 边双写时 | T17 前校准 episode 映射（不阻断 T16 双写接入） |
