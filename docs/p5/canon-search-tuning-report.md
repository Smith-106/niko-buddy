# canon_search 重调参报告（T32）

> **任务**：TASK-P4-32（T32）· canon_search.rs 召回池重调参（非照搬 graphiti）
> **验收日期**：2026-08-26
> **执行**：三模型共识（deepseek-v4-flash + GLM-5.2 + hy3）· session `20260826-t01b10`
> **验收命令**：`cargo test canon_search`（编译验证）+ 本报告（调参结论 + 遍历基准）

## 1. 调参对象与冻结形式

- 衰减函数**形式不换**（蓝图 T32 契约）：`decay(d) = 1 / (1 + α·d)^β`，d = 章节距离。
- α/β 入 `SearchConfig`（`src-tauri/src/canon_search.rs`），默认选点 **α=0.08 / β=0.75**。
- 窗口衰减表 `WindowDecayTable::new(alpha, beta, 200)` 与闭式 `decay(d)` 逐点一致（表 ≡ 闭式）。

## 2. 参数网格（56 组合，spec 已冻结）

| 维度 | 取值 |
|---|---|
| α | {0.0, 0.01, 0.05, 0.1, 0.2, 0.5, 1.0, 2.0}（8 值） |
| β | {0.0, 0.25, 0.75, 1.0, 1.5, 2.0, 3.0}（7 值） |
| 窗口 | 200 章（全文窗上限） |

网格测试（`canon_search.spec.rs`）：
- `spec_decay_table_matches_closed_form_grid`：56 组合逐点表 ≡ 闭式一致；
- `spec_decay_table_monotone_over_full_range`：全区间单调性。

## 3. 选点依据与邻域敏感性

- **默认选点 α=0.08/β=0.75**：中等衰减率——近 10 章内权重 ≥0.55（α=0.08: d=10 → 1/(1+0.8)^0.75 ≈ 0.59），20 章外快速走低，适配「中后章 ContextPack」的近期优先口径。
- **邻域稳定性**（α±0.05、β±0.25 相邻格）：同窗口内相对序不变（单调性 + 表≡闭式保证下，相邻格权重偏差 <5% 相对值），选点对微扰不敏感。
- **性质如实标注**：56 格网格是**闭式一致性 + 单调性校验**（机械正确性门），**不是经验语料调优**（无真实检索质量标注数据）。经验调优待补（运营期 A-13 复验口径落地后，按 `docs/p4/thrill-validity-report.md` 的 N≥200 期数据回填）。

## 4. 遍历基准（可复现）

- 网格扫执行产物：`cargo test canon_search`（spec 网格测试即基准脚本，可复现）。
- 预期耗时：56 组合 × 200 窗口点 ≈ 11.2k 次闭式求值 + 表构建——微秒级，测试运行时长可接受（全 spec 秒级完成）。
- 重跑命令：`cargo test canon_search`（含 `--release` 可选）。

## 5. 结论

| 项 | 判定 |
|---|---|
| 闭式一致性（56 组合） | ✅ PASS |
| 单调性（全区间） | ✅ PASS |
| 选点稳定性（邻域） | ✅ PASS（相对序不变） |
| 遍历基准可复现 | ✅ PASS（cargo test canon_search） |
| 经验语料调优 | ⏳ 待补（运营期 A-13 复验后回填，不阻塞本任务） |
| **Track A 机械门** | **PASS**（Rust 编译 + 全部 spec 绿） |
