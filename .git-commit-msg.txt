fix(shell): 把门与锁挂载进应用壳层（此前新增能力无任何入口）

可达性缺陷：本轮新增的 8 个面板（AppLockOverlay / ConfirmGateDialog /
CloudBackupPanel / McpTransportSettings / SkillPackPanel / BatchReplacePanel /
PdfExportDialog / SnapshotTimeline）均未被应用壳层挂载，src/App.tsx 与
app-layout.tsx 无 import，真实应用里点不到任何入口——全部能力只经 IPC 可达。

本次接线（F-004 应用锁 UI、B-F-001 确认弹窗/熔断提示）：
- 新增宿主 src/components/gate/GateHost.tsx：轮询 confirm_gate_pending +
  confirm_gate_loop_state，渲染 ConfirmGateDialog / GateHaltBanner；
  解除熔断只带 LoopState.halt_request_id 走既有 resume_after_halt，
  不新增任何判定逻辑；浏览器预览（非 Tauri）不轮询，查询失败静默。
- src/App.tsx 两个分支挂载 GateHost 与 AppLockOverlay，置于末尾：欢迎屏
  与主布局都是 fixed inset-0，先挂会被遮住指针事件。
- 宿主内包一层 relative：横幅无定位属性时仍会被 positioned 的壳层内容盖住。

测试：
- src/components/gate/GateHost.spec.tsx（6）：弹窗渲染与拒绝落地、熔断横幅与
  halt_request_id、缺 halt_request_id 不给恢复按钮、轮询拾起、enabled=false
  不轮询、查询失败静默。
- e2e/gate-host.spec.ts（3）：驱动真实壳层（锁屏遮挡/错口令/正确口令、
  弹窗可点击且拒绝后收回、熔断恢复只带 halt_request_id）。
- 同步修正 gate.spec.ts 与 app-lock.spec.ts 中已过期的「无宿主入口」声明。

验证：typecheck exit=0；lint exit=0（boundaries 4/4）；定向 vitest 74 passed
（5 files）；定向 playwright 13 passed。Rust 未改，cargo 404/0/2 复用。
